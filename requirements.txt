google-genai>=1.0.0
python-dotenv>=1.0.0
Pillow>=10.0.0
pyyaml>=6.0
# Optional provider SDKs:
# fal-client>=0.5.0      # pip install storyboard-gen[fal]
# replicate>=1.0.0       # pip install storyboard-gen[replicate]
